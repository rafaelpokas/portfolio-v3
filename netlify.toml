[build]
  command = "npm run build"
  publish = "out"

# Long cache for static assets (JS, CSS, fonts — hashed filenames)
[[headers]]
  for = "/_next/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
